# SPR-014 — Remote Control Authorization & Command Transport

## Objetivo

Implementar autorização temporária e transporte tipado de comandos de controle remoto entre dois
clientes. A autorização reutiliza o servidor de signaling; comandos trafegam exclusivamente no
RTCDataChannel existente e são somente registrados. Esta sprint não executa mouse ou teclado no
sistema operacional.

Também não são implementados clipboard, transferência de arquivos, chat, gravação ou múltiplos
participantes.

## Arquitetura

O módulo lógico solicitado em `client/src/core/remote-control` fica no workspace WebRTC
compartilhado, seguindo o mesmo mapeamento adotado para o RTC Engine:

```text
packages/engine/src/client/core/remote-control/
├── remote-control.manager.ts # Sessão, permissão e transporte P2P
├── permission.manager.ts     # State Machine e expiração
├── command.dispatcher.ts     # Codec, validação e executor de log
├── remote-control.service.ts # Fluxo distribuído de autorização
├── remote.events.ts          # EventType e grafo de estados
└── remote.types.ts           # Comandos, payloads e portas
```

Contratos usados no relay ficam em `packages/protocol/src/remote-control.ts`. O backend apenas
estende `services/backend/websocket/src/modules/signaling`; não cria gateway, sala, store ou conexão
paralela.

```text
Interface/composição
        │
RemoteControlService ──> PermissionManager ──> StateMachine<S>
        │                         │
        └──> RemoteControlManager ├──> CommandDispatcher ──> executor de log
                                  └──> DataChannelWebRtcService/DataChannelService
                                               │
                                  professor-connect-control
```

Todas as dependências externas são portas pequenas. O módulo não acessa `RTCPeerConnection`,
Socket.IO ou APIs do sistema operacional diretamente.

## Fluxo de autorização

```text
Professor                       Relay existente                         Aluno
   │                                  │                                   │
   ├── REMOTE_REQUEST ────────────────>│──────────────────────────────────>│
   │                                  │                        REQUESTED   │
   │<── REMOTE_ACCEPT ────────────────┤<──────────────────────────────────┤
   │ AUTHORIZED                       │             AUTHORIZED + expiresAt│
   ├── REMOTE_STARTED ───────────────>│──────────────────────────────────>│
   │ ACTIVE                           │                             ACTIVE│
   │<════════════ comandos pelo RTCDataChannel existente ════════════════>│
   │<── REMOTE_STOPPED/EXPIRED ──────┤<──────────────── revoga/expira ────┤
```

`authorizationId` correlaciona todo o ciclo. `durationMs` é solicitado pelo professor e o aluno
produz `expiresAt` no aceite. Ambos os clientes instalam um timer para o mesmo instante. Recusa
leva os dois lados a `DENIED`; revogação ou encerramento leva a `STOPPED`; expiração leva a
`EXPIRED`.

O `SignalingManager` continua validando Session ativa, Call ativa, dois clientes conectados,
pertencimento e participantes antes de encaminhar cada evento.

## Eventos e payloads

| `EventType`      | Transporte      | Payload                             |
| ---------------- | --------------- | ----------------------------------- |
| `REMOTE_REQUEST` | Socket.IO relay | `RemoteControlRequestPayload`       |
| `REMOTE_ACCEPT`  | Socket.IO relay | `RemoteControlAuthorizationPayload` |
| `REMOTE_DENY`    | Socket.IO relay | `RemoteControlReferencePayload`     |
| `REMOTE_STARTED` | Socket.IO relay | `RemoteControlReferencePayload`     |
| `REMOTE_STOPPED` | Socket.IO relay | `RemoteControlReferencePayload`     |
| `REMOTE_COMMAND` | RTCDataChannel  | `RemoteCommandTransportPayload`     |
| `REMOTE_EXPIRED` | Socket.IO relay | `RemoteControlReferencePayload`     |
| `REMOTE_FAILED`  | Socket.IO relay | `RemoteControlFailedPayload`        |

Todo evento usa o enum `EventType`. Eventos que passam pelo servidor usam obrigatoriamente
`SocketMessage<T>`. `REMOTE_COMMAND` também usa `SocketMessage<T>`, mas é serializado diretamente
no DataChannel e foi deliberadamente omitido dos mapas de eventos Socket.IO.

## Command Dispatcher e protocolo

Cada comando segue uma união discriminada e possui:

```text
commandId
type
timestamp
payload
```

Tipos disponíveis:

- `MouseMove`: coordenadas `x` e `y`;
- `MouseDown` e `MouseUp`: botão `LEFT`, `MIDDLE` ou `RIGHT`;
- `MouseWheel`: deltas `deltaX` e `deltaY`;
- `KeyDown` e `KeyUp`: `code`, `key` e `repeat`.

O `CommandDispatcher` valida todos os campos ao serializar e desserializar. Depois encaminha o
comando a `RemoteCommandExecutorPort`. O adapter padrão `LoggingRemoteCommandExecutor` somente
registra identificador e tipo; não produz nenhum efeito no mouse ou teclado.

O payload de transporte contém `authorizationId` e a representação serializada do comando. No
recebimento, o manager confere DataChannel da Call, Session do envelope, autorização, prazo e
estado `ACTIVE` antes de chamar o dispatcher.

## State Machine

Estados:

- `IDLE`;
- `REQUESTED`;
- `AUTHORIZED`;
- `ACTIVE`;
- `STOPPING`;
- `STOPPED`;
- `DENIED`;
- `EXPIRED`;
- `FAILED`.

```text
IDLE ──> REQUESTED ──> AUTHORIZED ──> ACTIVE ──> STOPPING ──> STOPPED
             │               │            │
             ├──> DENIED     ├────────────┴──> EXPIRED
             └───────────────────────────────> FAILED

STOPPED | DENIED | EXPIRED | FAILED ──> REQUESTED
```

Todas as transições passam por `StateMachine<RemoteControlState>` da Sprint 7. Um comando só é
permitido quando a máquina está em `ACTIVE` e a data atual ainda é anterior a `expiresAt`.

## Logs

São registrados: solicitação enviada, aceita ou recusada; sessão iniciada e encerrada; comando
enviado e recebido; permissão expirada; e falhas. Conteúdo sensível de teclas não é incluído no
contexto do logger; são registrados somente `commandId` e tipo.

## Como executar os testes

```bash
npm run test --workspace=@professor-connect/engine
npm run test --workspace=@professor-connect/websocket
npm run check
npm run build
```

Os testes de cliente conectam dois `DataChannelService` pelo contrato `DataChannelPort`, autorizam
professor e aluno, enviam os seis tipos de comando, verificam recebimento, revogação, recusa,
expiração e histórico de estados. Os testes Socket.IO sobem o servidor real e comprovam request,
accept, started e stopped pelo relay validado.

## Limites

Não existe executor de mouse ou teclado, captura de eventos do sistema, clipboard, arquivo, chat,
gravação ou múltiplos participantes. O módulo transporta intenções tipadas para permitir uma
futura sprint de execução com autorização explícita, sem antecipá-la agora.
